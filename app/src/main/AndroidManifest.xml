<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.activity">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:name="com.example.util.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.aidl.binder_pool.BinderPoolClientActivity">
        </activity>
        <activity android:name=".AnimatorTestActivity">
        </activity>

        <activity android:name=".FriendCircle">
        </activity>

        <activity android:name=".ContactActivity">
        </activity>

        <activity android:name=".SlidingMenuActivity">
        </activity>

        <activity android:name=".SwipeloadActivity">
        </activity>

        <activity android:name=".PermissionActivity">
        </activity>

        <activity android:name=".RecyclerViewActivity">

        </activity>

        <activity android:name=".DiActivity">

        </activity>

        <activity android:name=".ScopeTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.example.views.A1Activity">

        </activity>

        <service android:name="com.example.aidl.binder_pool.BinderPoolService" android:process=":remote">
        </service>

    </application>

</manifest>